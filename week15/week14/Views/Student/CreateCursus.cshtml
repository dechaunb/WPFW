@model week14.Models.Cursus

@{
    ViewData["Title"] = "CreateCursus";
}

<h1>Create</h1>

<h4>Cursus</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateCursus">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="cursusNaam" class="control-label"></label>
                <input asp-for="cursusNaam" class="form-control" />
                <span asp-validation-for="cursusNaam" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="CreateCursus" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="StudentIndex">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}